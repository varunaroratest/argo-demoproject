apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-Deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blue-Deployment
  template:
    metadata:
      labels:
        app: blue-Deployment
    spec:
      containers:
      - image: nginx:1.14.2
        imagePullPolicy: IfNotPresent
        name: nginx
